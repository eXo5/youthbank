
import React from 'react';
import {Col, Row, Card, CardTitle} from 'react-materialize';


class HeadCard extends React {
	constructor(){
		super()

		this.state ={
			currentKid: null
		}
	}

	componentDidMount(){
		axios.get("/api/get/chores").then(function(err,response =>{
		 	if(err){
		 		console.log(err);
		 	}else{
		 		console.log(response);
		 	}
		 })
		 	
	},
	



	render(){
		return(
			 <Card className='small'
              header={<CardTitle reveal image={background} waves="light"> Good Evening </CardTitle>}
              actions={[<a href='google.com'> </a>]}>
               Keep working on your goal for Concert Tickets!
            </Card>
			)
	}
}

export default HeadCard;